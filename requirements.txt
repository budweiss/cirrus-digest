# cirrus-digest — third-party Python dependencies (PINNED)
#
# Provisioning (docs/CIRRUS-CUMULUS-Environment-Plan.md §6):
#     python3 -m venv .venv && . .venv/bin/activate
#     pip install -r requirements.txt
#
# Scope: these are the third-party imports across the app
# (cirrus_*.py, dev_*.py, intake.py, pedagogy_daily.py, self_review.py,
# tools/*.py). Everything else the pipeline uses is Python stdlib.
# Ollama + models are installed separately (ollama pull qwen2.5:72b …),
# and whisper is pulled on demand. On CUMULUS/STRATUS the LLM SERVING
# stack (vLLM / TensorRT-LLM / Ollama-CUDA) is installed by provision.sh,
# NOT via this file — it is CUDA/arch-specific.
#
# Pins verified 2026-07-17 (Session 41) in a clean venv. All 5 are
# pure-Python or portable wheels — same versions install cleanly on
# macOS/Metal (CIRRUS) and ARM64/CUDA (CUMULUS/STRATUS).
# Re-pin with:  pip install -U <pkgs> && pip freeze | grep -iE '<pkgs>'

beautifulsoup4==4.15.0   # bs4 — HTML parsing (research ingest, feed scraping)
feedparser==6.0.12       # RSS/Atom feed parsing (cirrus_daily, cirrus_digest)
Flask==3.1.3             # cirrus_api admin API (port 5001)
numpy==2.2.6             # cirrus_rag vector store (nomic-embed-text embeddings)
requests==2.34.2         # everywhere — HTTP for feeds, Telegram, Ollama, Claude API
openpyxl                 # xlsx builders (newdev/build_report.py, snow workbooks) — S56: was
                         # missing here; pip-installed ad-hoc on CIRRUS, absent on CUMULUS.
                         # Unpinned so ARM64/CUDA + macOS both resolve a native wheel.

# --- Tested transitive set (captured 2026-07-17; informational) ---
# If you need a fully-locked build, pin these too. Left unpinned by
# default so ARM64/CUDA wheels resolve natively on CUMULUS/STRATUS:
#   blinker==1.9.0  certifi==2026.6.17  charset-normalizer==3.4.9
#   click==8.4.2  idna==3.18  itsdangerous==2.2.0  Jinja2==3.1.6
#   MarkupSafe==3.0.3  sgmllib3k==1.0.0  soupsieve==2.8.4
#   typing_extensions==4.16.0  urllib3==2.7.0  Werkzeug==3.1.8
